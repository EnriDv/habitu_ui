class CompletionConfirmationResult {
  final String? notes;
  final bool hasEvidence;
  final String? photoPath;

  const CompletionConfirmationResult({
    required this.notes,
    required this.hasEvidence,
    required this.photoPath,
  });
}
